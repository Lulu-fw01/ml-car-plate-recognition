[project]
name = "ml-car-plate-recognition"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "albumentations>=2.0.8",
    "grpcio-reflection>=1.78.0",
    "grpcio-tools>=1.78.0",
    "hydra-core>=1.3.2",
    "jiwer>=4.0.0",
    "mlflow>=3.1.4",
    "pyctcdecode>=0.5.0",
    "pytorch-lightning>=2.6.0",
    "rapidfuzz>=3.13.0",
    "scikit-learn>=1.6.1",
    "seaborn>=0.13.2",
    "torch>=2.8.0",
    "torchmetrics>=1.8.2",
    "torchvision>=0.23.0",
    "ultralytics>=8.4.48",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

dependencies = [
    "torch>=2.0",
    "torchvision",
    "pytorch-lightning>=2.0",
    "hydra-core>=1.3",
    "mlflow>=2.10",
    "dvc>=3.0",
    "albumentations>=1.4",
    "opencv-python>=4.9",
    "ultralytics>=8.2",          # YOLOv8/v10
    "numpy",
    "Pillow",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4",
    "pre-commit>=3.5",
    "pytest",
]

[project.scripts]
train-yolo = "scripts.train_yolo:main"
train-ocr = "scripts.train_ocr:main"
download-data = "data.download:cli"
